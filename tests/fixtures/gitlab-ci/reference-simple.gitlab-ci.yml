.rules:
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: always
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: manual

build:
  stage: build
  script:
    - echo "Building..."
  rules: !reference [.rules, rules]
